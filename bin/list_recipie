#!/usr/bin/env ruby

require 'net/http'
require File.dirname(__FILE__) + '/../lib/recipie_parser'

recipe = RecipieParser.parse(Net::HTTP.get(URI(ARGV[0])))


recipe.ingredient_items.each { | ingredient_item| puts ingredient_item.to_s }
